<!DOCTYPE html>
<html>
<head>
  <title>EmpregoJa</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
        <%= link_to 'Emprego Já', root_path, class: 'navbar-brand'%>
      </div>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if user_signed_in? %>
            <%= link_to ' Olá, ' + current_user.email, dashboard_path(current_user), class:'glyphicon glyphicon-cog'  %>
          <% end %>
        </li>
        <li><%= link_to ' Sign Up', new_user_registration_path , class:'glyphicon glyphicon-user'%></li>
        <li><%= link_to ' Login', new_user_session_path, class:'glyphicon glyphicon-log-in'%></li>
      </ul>
    </div>
  </nav>



<% flash.each do |key, value| %>
  <%=  value %> <br/>
<% end %>
<%= yield %>

</body>
</html>
